import './globals.css'
export const metadata={title:'TopSpeed Invoice & Reminder',description:'Invoice and reminder app'}
export default function RootLayout({children}){return <html lang="en"><body>{children}</body></html>}
